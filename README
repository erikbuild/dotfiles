# erik's dotfiles

install oh-my-zsh
install homebrew
brew install wget
brew install direnv
brew install git
set git configs
install ruby-on-mac ultimate
install asdf (node, elixir, etc)
```
rm ~/.zshrc
cd ~/
git clone git@github.com:grumpymaker/dotfiles.git
ln -s dotfiles/.zshrc
ln -s dotfiles/.claude
```
reload shell
